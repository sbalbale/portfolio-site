Create a Next.js Route Handler (`app/api/contact/route.ts`) using the `resend` SDK to process form submissions from the portfolio contact form.

It should accept a POST request containing `name`, `email`, and `message`. Return appropriate 200 success or 500 error JSON responses. Ensure proper TypeScript typing for the request body.
